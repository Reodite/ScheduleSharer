# ScheduleSharer

Share and view student registration schedules.

## Examples

The `examples/` folder contains sample student registration schedule exports (`.xlsx`) from the student registration system.
